/* Strand scorecard, styled to match globals.css: dark green ground, cream text,
   gold labels in DM Mono, and the same translucent badge style as .ground-tag. */
.card {
  --sc-muted: rgba(245, 240, 232, 0.5);
  --sc-line: rgba(255, 255, 255, 0.05);
  margin: 24px 0 8px;
  color: var(--cream);
}

.title {
  font-family: 'DM Mono', monospace;
  font-size: 0.62rem;
  font-weight: 500;
  letter-spacing: 0.08em;
  text-transform: uppercase;
  color: var(--gold);
  padding-bottom: 8px;
  margin-bottom: 12px;
  border-bottom: 2px solid var(--gold);
}

/* Pick box: same gold treatment as .tier-1 */
.pick {
  display: flex;
  flex-direction: column;
  gap: 2px;
  padding: 10px 14px;
  border-left: 3px solid var(--gold);
  background: rgba(201, 168, 76, 0.08);
  border-radius: 0 3px 3px 0;
}
.pickName {
  font-family: 'Bebas Neue', sans-serif;
  font-size: 1.5rem;
  letter-spacing: 0.04em;
  line-height: 1.1;
  color: var(--gold-light);
}
.pickMeta {
  font-family: 'DM Mono', monospace;
  font-size: 0.66rem;
  letter-spacing: 0.04em;
  color: var(--sc-muted);
}
.summary { margin: 10px 0 14px; font-size: 0.875rem; line-height: 1.55; color: rgba(245, 240, 232, 0.85); }

.head, .row { display: grid; gap: 4px; align-items: center; }
.head {
  padding: 6px 0;
  background: rgba(201, 168, 76, 0.12);
  border-bottom: 2px solid var(--gold);
}
.headLabel {
  writing-mode: vertical-rl;
  transform: rotate(180deg);
  height: 56px;
  justify-self: center;
  font-family: 'DM Mono', monospace;
  font-size: 0.56rem;
  letter-spacing: 0.08em;
  text-transform: uppercase;
  color: var(--gold);
}
.headTotal {
  justify-self: end;
  padding-right: 4px;
  font-family: 'DM Mono', monospace;
  font-size: 0.56rem;
  letter-spacing: 0.08em;
  text-transform: uppercase;
  color: var(--gold);
}

.list { list-style: none; margin: 0; padding: 0; }
.item { border-bottom: 1px solid var(--sc-line); }
.row {
  width: 100%;
  padding: 8px 0;
  background: none;
  border: 0;
  text-align: left;
  font: inherit;
  color: inherit;
  cursor: pointer;
}
.row:hover { background: rgba(255, 255, 255, 0.025); }
.row:focus-visible { outline: 2px solid var(--gold); outline-offset: 2px; }
.name { grid-column: 1 / -1; font-size: 0.9rem; font-weight: 600; margin-bottom: 3px; }
.draw { font-family: 'DM Mono', monospace; font-size: 0.7rem; font-weight: 400; color: var(--sc-muted); }

/* Cells follow the .ground-tag colours: green = in its favour, red = against, grey = neutral.
   Strong scores (±2) get a fuller fill and a border; ±1 are lighter. */
.cell {
  height: 22px;
  border-radius: 2px;
  display: flex;
  align-items: center;
  justify-content: center;
  font-family: 'DM Mono', monospace;
  font-size: 0.62rem;
  font-weight: 700;
  font-variant-numeric: tabular-nums;
}
.pos2    { background: rgba(39, 174, 96, 0.45);  color: #eafaf0; border: 1px solid rgba(46, 204, 113, 0.7); }
.pos1    { background: rgba(39, 174, 96, 0.18);  color: #2ecc71; }
.zero    { background: rgba(127, 140, 141, 0.18); color: #95a5a6; }
.neg1    { background: rgba(231, 76, 60, 0.18);  color: #e74c3c; }
.neg2    { background: rgba(231, 76, 60, 0.45);  color: #fdecea; border: 1px solid rgba(231, 76, 60, 0.8); }
.unknown { border: 1px dashed rgba(245, 240, 232, 0.2); color: rgba(245, 240, 232, 0.35); }

.total {
  justify-self: end;
  padding-right: 4px;
  font-family: 'DM Mono', monospace;
  font-size: 0.9rem;
  font-weight: 700;
  font-variant-numeric: tabular-nums;
  color: var(--gold);
}

.detail { margin: 0; padding: 4px 0 12px; font-size: 0.8rem; line-height: 1.5; }
.detailRow { display: grid; grid-template-columns: 76px 1fr; gap: 10px; padding: 2px 0; }
.detailRow dt {
  font-family: 'DM Mono', monospace;
  font-size: 0.58rem;
  letter-spacing: 0.08em;
  text-transform: uppercase;
  color: var(--gold);
  padding-top: 2px;
}
.detailRow dd { margin: 0; color: rgba(245, 240, 232, 0.8); }

.how { margin-top: 12px; font-size: 0.8rem; line-height: 1.55; color: var(--sc-muted); }
.how p + p { margin-top: 6px; }
.how summary {
  cursor: pointer;
  font-family: 'DM Mono', monospace;
  font-size: 0.62rem;
  letter-spacing: 0.08em;
  text-transform: uppercase;
  color: var(--gold);
  margin-bottom: 6px;
}
.how summary:focus-visible { outline: 2px solid var(--gold); outline-offset: 2px; }
